{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "App to view weather forecast for a location. Build with Node.js.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "weather"
  ],
  "author": "dnafication",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "request": "^2.88.0"
  }
}
